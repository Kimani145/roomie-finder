specVersion: "v1"
serviceId: "roomie-finder"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "roomie-finder-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
